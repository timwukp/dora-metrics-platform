apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dora-metrics
resources:
  - namespace.yaml
  - serviceaccounts.yaml
  - networkpolicy.yaml
  - postgres.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml
  # Choose one secrets source:
  #   - external-secrets.yaml   (production, AWS Secrets Manager)
  #   - secrets.yaml            (gitignored, dev only — copy from secrets.example.yaml)
