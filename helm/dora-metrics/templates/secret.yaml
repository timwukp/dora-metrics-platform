{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: dora-secrets
  namespace: {{ include "dora.namespace" . }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secret.values }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end }}
